[project]
name = "keycloak-test-client"
version = "0.1.0"
description = "Minimal test client for Keycloak OAuth 2.0 client credentials flow"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]
